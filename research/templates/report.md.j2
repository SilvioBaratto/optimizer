# Research Run Report

**Assembly hash:** `{{ assembly_hash }}`

## 1. Regime & Tilts

**Current regime:** `{{ regime }}`

| Factor | Tilt |
| --- | ---: |
{% for factor, tilt in tilts.items() -%}
| {{ factor }} | {{ "%.4f"|format(tilt) }} |
{% endfor %}

## 2. Factor IS / OOS IC

| Factor | IS Mean IC | IS t-stat | IS Significant | OOS Mean IC |
| --- | ---: | ---: | :---: | ---: |
{% for row in factor_ic_rows -%}
| {{ row.factor }} | {{ "%.4f"|format(row.is_mean_ic) }} | {{ "%.2f"|format(row.is_t_stat) }} | {{ "yes" if row.is_significant else "no" }} | {{ "%.4f"|format(row.oos_mean_ic) }} |
{% endfor %}

## 3. Optimizer Config Diff vs Default

{% if optimizer_diff -%}
| Field | Value |
| --- | --- |
{% for key, value in optimizer_diff.items() -%}
| {{ key }} | `{{ value }}` |
{% endfor %}
{%- else -%}
_No deviations from defaults._
{%- endif %}

## 4. Binding Constraints

{% if binding_constraints -%}
{% for c in binding_constraints -%}
- {{ c }}
{% endfor %}
{%- else -%}
_None._
{%- endif %}

## 5. Top-4 Retighten Trace

{% if retighten_trace -%}
| Attempt | Top-4 weight | max_weights |
| ---: | ---: | ---: |
{% for entry in retighten_trace -%}
| {{ entry.attempt }} | {{ "%.4f"|format(entry.top4) }} | {{ "%.6f"|format(entry.max_weights) }} |
{% endfor %}
{%- else -%}
_No retighten attempts (Top-4 below threshold on first fit)._
{%- endif %}

## 6. Hybrid Rebalance Decision

- **Rebalance:** {{ "yes" if rebalance_decision[0] else "no" }}
- **Reason:** `{{ rebalance_decision[1] }}`

## 7. 17-Rule Checklist

| # | Rule | Pass | Measured | Target |
| ---: | --- | :---: | --- | --- |
{% for row in checklist_rows -%}
| {{ loop.index }} | {{ row.rule }} | {{ "yes" if row.pass else "no" }} | `{{ row.measured }}` | `{{ row.target }}` |
{% endfor %}

## 8. Metrics

{% for group, kpis in metrics.items() -%}
### {{ group }}

| KPI | Value |
| --- | ---: |
{% for kpi, val in kpis.items() -%}
| {{ kpi }} | {{ "%.4f"|format(val) }} |
{% endfor %}

{% endfor %}

## 9. Charts

{% for path in chart_paths -%}
- ![{{ path.stem }}](./{{ path.name }})
{% endfor %}
